<form method="post" action="{{ path('equipment_handover_delete', {'id': equipment_handover.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipment_handover.id) }}">
    <button class="btn">Delete</button>
</form>
